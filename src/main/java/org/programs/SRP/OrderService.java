package org.programs.SRP;

// Class only responsible for order placement
public class OrderService {
    public void placeOrder(String itemId, int qty) {
        System.out.println("Placing order for item: " + itemId);
    }
}
